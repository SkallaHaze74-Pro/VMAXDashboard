# Archivierte Workflows

`vmaxbuild-apk.yml` ist der frühere 6.6-Testbuild. Die Datei liegt außerhalb von `.github/workflows/` und kann deshalb nicht als GitHub Action gestartet werden. Releases werden ausschließlich über **VMAX Dashboard Fahrdaten Build** erzeugt.
